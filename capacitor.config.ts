import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ba.studyplanner',
  appName: 'Studyplanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
